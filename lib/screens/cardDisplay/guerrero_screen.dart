import 'package:flutter/material.dart';

class GuerreroScreen extends StatelessWidget {
  const GuerreroScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('guerrero'));
  }
}
